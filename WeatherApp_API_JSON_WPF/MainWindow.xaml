<Window x:Class="WeatherApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aplikacja pogodowa" Height="400" Width="400">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Wprowadzanie miasta -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="CityTextBox" Width="200" Height="30" VerticalAlignment="Center" 
                     Margin="0,0,10,0" Text="Kraków"/>
            <Button Content="Pobierz pogodę" Width="150" Click="GetWeather_Click"/>
        </StackPanel>

        <!-- Wyniki -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Image x:Name="WeatherIcon" Width="50" Height="50" Margin="0,0,10,0"/>
            <StackPanel>
                <TextBlock x:Name="TemperatureText" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="PressureText" FontSize="14"/>
                <TextBlock x:Name="HumidityText" FontSize="14"/>
                <TextBlock x:Name="DescriptionText" FontSize="14" FontStyle="Italic"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>